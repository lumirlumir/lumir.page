{
  "extends": "../../tsconfig.base.app.json",
  "references": [
    {
      "path": "../../packages/react-kit/tsconfig.build.json"
    },
    {
      "path": "../../packages/utils/tsconfig.build.json"
    }
  ],
  "compilerOptions": {
    /* Modules */
    "paths": {
      "@/*": ["./src/*"] // Map the '@' alias to the 'src' directory.
    },

    /* Type Checking */
    "noImplicitAny": false, // TODO: Enable this in the future for better type safety.

    /* Modules */
    "types": ["@lumir/types/global", "node"]
  }
}
