{
  "private": true,
  "name": "api",
  "type": "module",
  "engines": {
    "node": "^24.15.0"
  },
  "scripts": {
    "build": "tsgo -b tsconfig.build.json",
    "dev": "node --env-file=.env --watch ./src/server.ts"
  },
  "dependencies": {
    "openai": "^6.38.0"
  }
}
