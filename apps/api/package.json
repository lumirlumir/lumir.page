{
  "private": true,
  "name": "api",
  "type": "module",
  "engines": {
    "node": "^24.18.0"
  },
  "scripts": {
    "build": "tsgo -b tsconfig.build.json",
    "dev": "node --env-file=.env --watch ./src/dev/server.ts",
    "req": "node ./src/dev/client.ts",
    "test": "vitest run"
  },
  "devDependencies": {
    "@lumir/types": "*",
    "vitest": "^4.1.9"
  }
}
